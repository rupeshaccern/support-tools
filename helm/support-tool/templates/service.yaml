apiVersion: v1
kind: Service
metadata:
  name: {{ include "support-tool.fullname" . }}
  labels:
    {{- include "support-tool.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "support-tool.selectorLabels" . | nindent 4 }}
